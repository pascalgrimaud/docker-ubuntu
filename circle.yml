machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t pascalgrimaud/ubuntu .
test:
  override:
    - docker run -it pascalgrimaud/ubuntu echo "Hello World !"
